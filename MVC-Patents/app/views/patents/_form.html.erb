<%= form_with(model: patent, local: true) do |form| %>
  <% if patent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(patent.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% patent.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>
  <div class="field">
    <%= form.label :body %>
    <%= form.text_field :body %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
